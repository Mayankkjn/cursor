# cursor

## Process Map Visualizer

`process-map/` is a standalone process mining–style visualization: a directly-follows
graph mined from a sample event log, showing the most frequent path through a
multi-step business process, deviations/alternative paths, and per-task duration.
Open `process-map/index.html` in a browser (no build step required).
